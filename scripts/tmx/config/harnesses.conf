# Harness registry — one line per harness: name|command
# $PORT is expanded at runtime from the session's OPENCODE_PORT.
# To add a new harness, add a line here. No other changes needed.
oc|opencode --port $PORT
cc|claude
cx|codex --yolo
